1. {some app} & disown - To run an app without holding up the terminal
https://unix.stackexchange.com/questions/200169/run-application-without-holding-up-terminal

2. qtile config reference: https://gitlab.com/OldTechBloke/dotfiles/-/blob/master/.config/qtile/config.py

3. chmod +x {file name} - To make something exectable

4. To install a binary of some sort instead of searching for appimage search for "bin" version. Then,
when you have finally downloaded your package, run "sudo pacman -U {package name}" command

5. in case if apps in the qtile tray are on different monitors, run meta+ctrl+r to reload qtile config

6. vscode launches on "code" command

7. arecord -l and aplay -l for listing of capture and playback hardware devices


Todo:
 - xbacklight nvidia

Key bindings:

Alacritty:
 - ctrl+=, ctrl++ - increase fontsize (zoom in)
 - ctrl+- - decrease fontsize (zoom out)
 - super+v - paste
